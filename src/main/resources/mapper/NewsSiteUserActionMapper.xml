<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.advertisement_system.mapper.NewsSiteUserActionMapper">
    <insert id="insertUserAction">
        INSERT INTO news_user_profile (user_id, tag_name, tag_weight)
        VALUES (#{userId}, #{tagName}, #{tagWeight})
    </insert>
</mapper>