spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/adv_system?serverTimezone=GMT%2B8
    username: root
    password: bdbg2181

mybatis:
  configuration:
    log-impl=org:
      apache:
        ibatis:
          logging:
            stdout:
              StdOutImpl:

  mapper-locations: classpath:mapper/*.xml
  mybatis:
    configuration:
      map-underscore-to-camel-case=true:
server:
  port: 8090